import 'package:flutter/material.dart';

class SetupInterest extends StatelessWidget {
  const SetupInterest({super.key});

  static const routeName = 'setup-interest';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
